{
  "name": "krebit",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "private": true,
  "packageManager": "yarn@1.22.18",
  "scripts": {
    "build": "turbo run build",
    "dev": "dotenv -e .env -- turbo run dev --parallel --no-cache",
    "clean": "turbo run clean && rm -rf node_modules && rm -rf logs",
    "node-dev": "dotenv -e .env -- turbo run start --filter=node --no-cache",
    "node-api": "dotenv -e .env -- turbo run dev --filter=node --no-cache",
    "node-start": "turbo run start --filter=node --no-cache"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/KrebitDAO/krebit.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/KrebitDAO/krebit/issues"
  },
  "homepage": "https://github.com/KrebitDAO/krebit#readme",
  "devDependencies": {
    "dotenv-cli": "^5.1.0",
    "turbo": "^1.2.9"
  },
  "dependencies": {
    "@changesets/cli": "^2.23.1"
  }
}
